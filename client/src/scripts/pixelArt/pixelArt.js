import {
    SELECTOR,
    ELEMENTS,
    COLORS,
    LENGTH,
} from './constants.js';

import {
    UTILS,
} from '../constants/utils.js';


const {
    CREATE_EL,
} = UTILS;

function PixelArt(el, rows, cols) {
    this.init();
}

PixelArt.prototype = {
    init: function () {
        const input = document.createElement;
    }
};
